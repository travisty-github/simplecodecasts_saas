<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <h1>DevMatch Community</h1>
    <p>Welcome to the DevMatch community. Users should fill out a profile in
    order to be visible.</p>
    <ul class="list-unstyled">
      <% @users.each do |u| %>
        <% if u.profile %>
          <li>
            <div class="well row <%= cycle('white-bg', '') %>">
              <div class="col-sm-4 text-center">
                <%= link_to image_tag(u.profile.avatar.url(:thumb),
                    class: 'avatar'), u %>
              </div>
              <div>
                <%= link_to u do %>
                  <h2><%= u.profile.first_name %> <%= u.profile.last_name %></h2>
                <% end %>
                <p><%= u.profile.job_title %> <%= pro_label(u) %></p>
              </div>
            </div>
          </li>
        <% end %>
      <% end %>
    </ul>
  </div>
</div>
